using System;
using System.Collections.Generic;
using System.Dynamic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using System.Drawing;
using NUnit.Framework;
using Microsoft.AspNetCore.Mvc;
using System.Security.Claims;
using Bakalauras.Models;
using Bakalauras.Data;
using MockQueryable.Core;
using System.Data.Entity;
using Moq;
using Bakalauras.Pages.Testai;
using Microsoft.AspNetCore.Mvc.RazorPages;

namespace BakalaurasTests
{
    public class IntegrationTesting
    {
        [Test]
        public async void RunBlet()
        {
            string userIrd = "test";
            var mockDbContext = new Mock<ApplicationDbContext>();

            var pageModel = new CreateTestModel(mockDbContext.Object);
            var items = await pageModel.OnGet(userIrd);

            Assert.IsTrue(pageModel._Tasks.Count() > 0);
        }
    }
}